function scrolll() {
    var left = document.querySelector('.caseMain__scroll-cover-img');
    left.scrollBy(-350, 0);
}

function scrollRight() {
    var right = document.querySelector('.caseMain__scroll-cover-img');
    right.scrollBy(350, 0);
}


// Activation Ajinomoto
function scrolllActivation() {
    var left = document.querySelector('.scroll-activation-img');
    left.scrollBy(-350, 0);
}

function scrollRightActivation() {
    var right = document.querySelector('.scroll-activation-img');
    right.scrollBy(350, 0);
}

// Activation Gốm sứ Minh Long
function scrolllActivationML() {
    var left = document.querySelector('.scroll-activation-img2');
    left.scrollBy(-350, 0);
}

function scrollRightActivationML() {
    var right = document.querySelector('.scroll-activation-img2');
    right.scrollBy(350, 0);
}